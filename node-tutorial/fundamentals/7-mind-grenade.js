const num1 = 5;
const num2 = 10;

function addTwo() {
  console.log(`the sum is: ${num1 + num2}`);
}

addTwo();

// if you have invoked the function then you don't have to export it and you can still use require in other modules
